import React from "react";
import preloader from "../../../assets/img/load.svg";

let Preloader = (props) => {
  return (
    <div>
      <img src={preloader} />
    </div>
  );
};

export default Preloader;
